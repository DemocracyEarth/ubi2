[package]
name = "ubi2-runtime"
description = "ubi2 deterministic runtime: accounts, streaming emission, demurrage, streams."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
