{
  "name": "@ubi2/status-operator",
  "version": "0.0.0",
  "private": true,
  "description": "Fail-closed packed-status reconciler and artifact service for ubi2 v2 identity.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx src/status-operator.test.ts && tsx src/readiness.test.ts && tsx src/host-attestation.test.ts",
    "test:cast": "tsx src/cast-adapter.test.ts",
    "start": "tsx src/cli.ts"
  },
  "dependencies": {
    "@ubi2/sdk": "workspace:*",
    "tsx": "^4.22.4",
    "viem": "^2.53.1"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.5.0"
  }
}
